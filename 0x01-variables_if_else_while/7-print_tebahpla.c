#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: 0
 *
 */

int main(void)
{
	int low;

	for (low = 'z'; low >= 'a'; low ==)
		putchar(low);
	putchar("\n");
	return (0);
}

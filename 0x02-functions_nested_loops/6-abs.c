#include "main.h"

/**
 * _abs -> prints absolute value of a number
 * @n: parameter
 * Return: absolute value of a number
 */

int _abc(int n)

{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else
	return(c * -1);
	
}

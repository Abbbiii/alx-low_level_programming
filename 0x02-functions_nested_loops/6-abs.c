#include <stdio.h>
#include <limits.h>
#include "main.h"

/**
 * _abs -> prints absolute value of a number
 * @c: is the int that will be used for the argument
 * Return: 0
 */

int _abc(int c)

{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else
	return(c * -1);
	
}
